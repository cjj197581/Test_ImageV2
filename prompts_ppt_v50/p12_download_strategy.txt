A clean automated pipeline flowchart diagram for a presentation slide about "AI-Driven Automated Paper Download Strategy". Show a horizontal pipeline with connected nodes: "检索" (Search) → "下载" (Download) → "校验 SHA256" (Verify) → "分类归档" (Classify) → "元数据提取" (Metadata Extract). Each node should be a rounded rectangle with an icon. Show "双批命令进程" (Dual Batch Process) as two parallel streams. Include stats: "每分钟4篇", "日下载5000篇", "24小时不间断". Light blue and white color scheme with deep blue (#0050B3) and bright cyan (#00B4D8) accents. Modern flat tech flowchart style. Chinese text labels. 16:9 wide format for professional presentation.